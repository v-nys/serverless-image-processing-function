version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  background-removal:
    lang: node18
    handler: ./background-removal
    image: vincentnys/background-removal:latest
    environment:
      NODE_ENV: production
    secrets:
      - "auth-token"
